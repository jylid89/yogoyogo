<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="review">

<!-- 리뷰리스트 -->
<select id="reviewList" resultType="yogo.review.dto.ReviewVO"> 
	SELECT r.rev_title rev_title, r.rev_content rev_content, r.rev_date rev_date, m.mem_nick mem_nick
	FROM 	tb_review r , tb_member m
	WHERE 	r.mem_id = m.mem_id
</select>


</mapper>